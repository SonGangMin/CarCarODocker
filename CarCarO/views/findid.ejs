<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
  </head>
  <body>
    <div align="center">
      <h1>아이디 찾기</h1>
      <form action="/auth/findid" id="findid" method="post" class="findid">
        <input type="text" name="name" placeholder="이름을 입력해주세요" />
        <input
          id="email"
          type="email"
          name="email"
          placeholder="e-mail 입력해주세요"
          required
        />
        <button type="submit">제출</button>
        <br />
      </form>
    </div>

    <!-- <script>
      const findid = document.querySelector(".findid");

      findid.addEventListener("click", (event) => {
        event.preventDefault();
        const emailValue = document.getElementById("email").value;

        fetch(`/findID?email=${emailValue}`)
          .then((response) => response.json())
          .then((data) => {
            alert(`찾으시는 아이디는 ${data.id}입니다.`);
          })
          .catch((error) => {
            console.error("Error:", error);
          });
      });
    </script> -->
  </body>
</html>
